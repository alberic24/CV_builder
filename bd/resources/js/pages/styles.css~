/* styles.css */

/* General container styling */
.fill-cv-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background-color: #f5f5f5; /* Light gray background */
    padding: 20px;
}

/* Form styling */
.cv-form {
    display: flex;
    width: 100%;
    max-width: 1200px;
    background: #ffffff; /* White background for form */
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
}

/* Left section styling */
.cv-form-left {
    width: 40%;
    padding: 20px;
    border-right: 1px solid #ddd; /* Light gray border */
    box-sizing: border-box;
}

.cv-form-left label {
    font-weight: bold;
    display: block;
    margin-bottom: 5px;
}

.cv-form-left input[type="text"],
.cv-form-left input[type="file"],
.cv-form-left textarea {
    width: calc(100% - 20px);
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
}

/* Right section styling */
.cv-form-right {
    width: 60%;
    padding: 20px;
    box-sizing: border-box;
}

.cv-form-right label {
    font-weight: bold;
    display: block;
    margin-bottom: 5px;
}

.cv-form-right input[type="text"],
.cv-form-right textarea {
    width: calc(100% - 20px);
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
}

/* Buttons container styling */
.buttons-container {
    text-align: center;
    padding: 20px;
    background: #f9f9f9;
    border-top: 1px solid #ddd; /* Light gray border */
}

.pdf-btn {
    background-color: #009151; /* Chocolate color */
    color: white;
    border: none;
    border-radius: 4px;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
}

.pdf-btn:hover {
    background-color: #007a40; /* Darker shade of chocolate color */
}

.pdf-btn:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(0, 145, 115, 0.5); /* Focus outline */
}

/* Responsive design */
@media (max-width: 768px) {
    .cv-form {
        flex-direction: column;
    }

    .cv-form-left, .cv-form-right {
        width: 100%;
        border-right: none;
        border-bottom: 1px solid #ddd; /* Border between sections */
    }

    .cv-form-left {
        border-bottom: none;
    }
}
